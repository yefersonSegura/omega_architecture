# Testing

Omega keeps logic in flows and agents so you can unit-test **without** pumping a full widget tree when you do not need to.

Use **`flutter test`** on your app; drive **`OmegaChannel`**, **`OmegaFlowManager`**, and agents without widgets when possible. See **`test/`** in this package repository for flow/agent tests.
